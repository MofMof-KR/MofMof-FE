import styled from 'styled-components';

export const Header = styled.header`
  padding: 2.06rem 1rem 1.09rem;
  display: flex;
  flex-flow: row nowrap;
  justify-content: space-between;
  align-items: center;
`;

export const Heading = styled.h1`
  flex: 1 1 auto;
  text-align: center;
  font-weight: 700;
  color: var(--gray-7);
  font-family: Pretendard;
  font-size: 1.375rem;
  font-style: normal;
  font-weight: 600;
  line-height: 1.75rem;
`;
