import styled from 'styled-components';

export const SignUpEndSection = styled.section`
  height: 100%;
  display: flex;
  flex-flow: column nowrap;
  justify-content: center;
  align-items: center;
`;

export const Span = styled.span`
  color: var(--black);
  text-align: center;
  font-size: 1.5rem;
  font-weight: 600;
  line-height: 2.125rem;
  white-space: pre-wrap;
  padding: 1rem 0;
`;

export const P = styled.p`
  color: var(--gray-6);
  text-align: center;
  line-height: 1.5rem;
`;
