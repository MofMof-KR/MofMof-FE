import styled from 'styled-components';

export const P = styled.p`
  display: block;
  margin: 1.56rem auto;
  text-align: center;
  color: var(--gray-6);
  font-size: 0.875rem;
  font-weight: 400;
  line-height: 1.25rem;
  white-space: pre-wrap;
`;
